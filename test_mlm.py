from src.nlp.spelling.contextual_corrector import ContextualCorrector
import logging
logging.basicConfig(level=logging.INFO)

c = ContextualCorrector()
print("Contextual Corrector loaded successfully!")
